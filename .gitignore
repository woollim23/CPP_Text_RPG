# =====================================================
#  화이트리스트 방식 .gitignore
#  기본적으로 모든 파일을 무시하고,
#  빌드/실행에 꼭 필요한 파일만 명시적으로 허용한다.
# =====================================================

# 1) 기본: 모든 파일 무시
*

# 2) 폴더는 탐색 허용 (안에 있는 허용 파일을 찾으려면 필요)
!*/

# -----------------------------------------------------
#  Git 자체 설정 파일
# -----------------------------------------------------
!.gitignore
!.gitattributes

# -----------------------------------------------------
#  Visual Studio 솔루션 / 프로젝트 (빌드에 필수)
# -----------------------------------------------------
!*.sln                  # 솔루션 파일 (Visual Studio 진입점)
!*.vcxproj              # 프로젝트 파일 (소스 목록, 빌드 설정)
!*.vcxproj.filters      # 솔루션 탐색기 폴더 구조

# -----------------------------------------------------
#  C++ 소스 코드
# -----------------------------------------------------
!*.cpp
!*.h
!*.hpp
!*.c
!*.cc
!*.cxx
!*.inl

# -----------------------------------------------------
#  문서 (있을 경우만)
# -----------------------------------------------------
!README.md
!LICENSE
!*.md

# -----------------------------------------------------
#  안전망: 위 화이트리스트와 비슷해 보이지만
#  실제로는 사용자별 설정이라 절대 올라가면 안 되는 파일
# -----------------------------------------------------
*.vcxproj.user          # 디버깅 환경 설정 등 개인 설정

# =====================================================
#  나중에 게임 데이터(.txt, .json 등)를 추가하게 되면
#  아래처럼 한 줄씩 화이트리스트에 추가하면 된다.
#  예시:
#    !*.txt
#    !*.json
#    !assets/**
# =====================================================
